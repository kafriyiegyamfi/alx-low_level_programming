include "main.h"

/**
 * _strlen - function that returns a strings length.
 *
 * @s: this is the input string
 *
 * Return: length of string
 *
 */

int _strlen(char *s)
{
int index;
for (index = 0; s[index] != '\0'; index++)

return (index);
}
